EXPR = {WS | NONE, INSTRUCTION | LABEL | COMMAND_NAME | COMMAND_DESC | NONE, WS | NONE, COMMENT | EMPTY_LINE | ENDLINE}
ERR = {DIR | IND | REG | LABEL_TAG | LABEL_NAME | NAME_CMD_STRING | COMMENT_CMD_STRING, EMPTY_LINE | ENDLINE}
COMMAND_NAME = {NAME_CMD_STRING, WS, STRING}
COMMAND_DESC = {COMMENT_CMD_STRING, WS, STRING}
EMPTY_LINE = {WS | NONE, NEWLINE, EMPTY_LINE | NONE}
COMMENT = {WS | NONE, COMMENT_CHAR, ASCII, EMPTY_LINE, COMMENT | NONE}
LABEL = {LABEL_TAG, (WS, INSTRUCTION) | (COMMENT | EMPTY_LINE, WS | NONE, LABEL | INSTRUCTION)}
LABEL_TAG = {LABEL_NAME, LABEL_CHAR}
LABEL_NAME = {LABEL_STRING}
INSTRUCTION = {LIVE | LDI | STI | ADD | SUB | AND | OR | XOR | ZJMP | FORK | LD | ST | LLDI | LLD | LFORK | AFF}
DIR = {DIRECT | DIRECT_LABEL}
IND = {INDIRECT | INDIRECT_LABEL}
REG = {REG_CHAR, REG_NBR}
SEP = {WS | NONE, SEPARATOR, WS | NONE}
DIRECT = {DIRECT_CHAR, NUMBER}
DIRECT_LABEL = {DIRECT_CHAR, LABEL_CHAR, LABEL_STRING}
INDIRECT = {NUMBER}
INDIRECT_LABEL = {LABEL_CHAR, LABEL_STRING}
LIVE = {LIVE_NAME, WS, DIR}
LD = {LD_NAME, WS, DIR | IND, SEP, REG}
ST = {ST_NAME, WS, REG, SEP, IND | REG}
ADD = {ADD_NAME, WS, REG, SEP, REG, SEP, REG}
SUB = {SUB_NAME, WS, REG, SEP, REG, SEP, REG}
AND = {AND_NAME, WS, REG | DIR | IND, SEP, REG | DIR | IND, SEP, REG}
OR = {OR_NAME, WS, REG | DIR | IND, SEP, REG | DIR | IND, SEP, REG}
XOR = {XOR_NAME, WS, REG | DIR | IND, SEP, REG | DIR | IND, SEP, REG}
ZJMP = {ZJMP_NAME, WS, DIR}
FORK = {FORK_NAME, WS, DIR}
LDI = {LDI_NAME, WS, REG | DIR | IND, SEP, DIR | REG, SEP, REG}
STI = {STI_NAME, WS, REG, SEP, REG | DIR | IND, SEP, DIR | REG}
LLD = {LLD_NAME, WS, DIR | IND, SEP, REG}
LLDI = {LLDI_NAME, WS, REG | DIR | IND, SEP, DIR | REG, SEP, REG}
LFORK = {LFORK_NAME, WS, DIR}
AFF = {AFF_NAME, WS, REG}
